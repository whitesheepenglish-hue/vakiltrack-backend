<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Gpt" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">#D0E4FF</item>
        <item name="colorOnPrimaryContainer">#001D34</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gold</item>
        <item name="colorOnSecondary">@color/dark_blue</item>
        <item name="colorSecondaryContainer">#FFDDAF</item>
        <item name="colorOnSecondaryContainer">#281800</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/dark_blue</item>

        <!-- Surface and Background -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/dark_blue</item>
        <item name="android:windowBackground">@color/white</item>

        <!-- Required Material3 Type Attributes -->
        <item name="textAppearanceHeadline6">@style/TextAppearance.Material3.HeadlineSmall</item>
        <item name="textAppearanceSubtitle1">@style/TextAppearance.Material3.BodyLarge</item>
        <item name="textAppearanceBody2">@style/TextAppearance.Material3.BodyMedium</item>
        <item name="textAppearanceButton">@style/TextAppearance.Material3.LabelLarge</item>

        <!-- Standard Material3 Styles -->
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.App.MaterialAlertDialog</item>
    </style>

    <style name="Theme.Gpt.AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar" />

    <style name="BottomSheetDialogTheme" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Material3.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="CircleImage" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- Professional Dropdown Style -->
    <style name="Widget.App.ExposedDropdownMenu" parent="Widget.Material3.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.ExposedDropdownMenu</item>
        <item name="shapeAppearance">@style/ShapeAppearance.App.MediumComponent</item>
    </style>

    <style name="ThemeOverlay.App.ExposedDropdownMenu" parent="">
        <item name="colorSurface">@color/white</item>
    </style>

    <style name="ShapeAppearance.App.MediumComponent" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Professional AlertDialog Style -->
    <style name="ThemeOverlay.App.MaterialAlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="shapeAppearance">@style/ShapeAppearance.App.MediumComponent</item>
        <item name="colorSurface">@color/white</item>
    </style>
</resources>
